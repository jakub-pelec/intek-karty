# Idea: synergy scope exceptions

**Status:** parked. Not this brief.

**Live default (current brief):** every synergy counts **all revealed cards on the table** (both boards). That is the Cat + every Food walkthrough.

**Later:** some cards **exception** to that default — extra points from **only my** matching cards, or **only the enemy’s**. Authoring, UI, and how that is written on the relic are unspecified.

Do not ship scope as a required field in the current pass. Rediscover before adding the exception cards.
